# T1 — TOPOLOGY (aktuální stav)

> **PENDING.** Tento soubor vygeneruje `tools/gateway/discover.sh` spuštěný na
> VPS a Macu (.43). Z cloud sandboxu nelze (žádný CF token / Tailscale / SSH).
>
> Spusť na nodu:
> ```bash
> CLOUDFLARE_API_TOKEN=… ./tools/gateway/discover.sh
> ```
> Doplní: CF DNS+tunely, reference na mrtvý RPi 192.168.1.220, docker/proxy,
> Tailscale stav, dosah na Mac Bible/Registry.
